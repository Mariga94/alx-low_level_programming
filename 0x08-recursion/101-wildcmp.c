#include "main.h"

/**
 * wildcmp - compares 2 strings
 * @s1: string 1
 * @s2: string 2
 *
 * Return: 1 if the string are identical Otherwise 0.
 */
int wildcmp(char *s1, char *s2)
{
	if (s1 == s2)
	{
		return (1);
	}

	return (0);
}
