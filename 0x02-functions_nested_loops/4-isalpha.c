#include "main.h"

/**
 * int_isalpha - checks alphabetic character
 * isalpha - checks if the passed character is alphabetic.
 * @a - int parameter
 * Return: 1
 */

int isalpha(int a);
int _isalpha(int c)
{
	if (isalpha(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}

	return (0);
}

